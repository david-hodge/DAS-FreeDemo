# Statistics Investigation Demo

This repo demonstrates a simple project structure for a data analysis workflow.

## Structure
- `data/` – raw and processed datasets
- `scripts/` – analysis scripts
- `notebooks/` – Jupyter notebooks
- `reports/` – final outputs and figures
